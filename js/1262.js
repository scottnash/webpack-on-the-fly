window.modules["1262"] = [function(require,module,exports){'use strict';
/**
 * This is the client-side implementation of the crypto service, which is being
 * scaffolded here as a placeholder for the server-side service, which imports
 * node/crypto.
 *
 * Functionality does not exist at this time, but should be developed as needed
 * to support the same function signatures as server/crypto. Options for doing so
 * may rely on window.crypto on the browesr, or to import an alternate NPM package
 */

module.exports.createHmac = function () {
  // placeholder function for the time being
  // usage of this function should be wrapped in try/catch
  throw Error('Client-side crypto service is not available right now');
};
}, {}];
